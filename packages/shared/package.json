{
  "name": "@rentrent/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:watch": "tsc --watch --preserveWatchOutput",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "dev": "tsc --watch --preserveWatchOutput",
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "typescript": "^5.2.2"
  }
}